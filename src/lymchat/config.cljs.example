(ns lymchat.config
  (:require [lymchat.util :refer [development?]]))

;; add stage server, maybe ngrok?
(defn api-host
  []
  (if (development?)
    "http://192.168.1.114:8089"
    "https://api.lymchat.com"))

(defonce xxxxx (atom nil))

(def RCTConfig (clj->js
                {:app_key "app-key"
                 :app_secret "app-secret"}))
